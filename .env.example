# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# Optional: set a secret token to lock the UI.
# If left empty, the app is open to anyone who can reach the server.
# On the server, generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
AUTH_TOKEN=
